# Core GUI dependencies
PySide6>=6.5.0

# Network and parsing dependencies
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.2.0

# P3: browser TLS impersonation for downloads/fallback (opt-in http_engine)
curl_cffi>=0.14.0

# HTML/XML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Media detection
filetype>=1.2.0

# Character encoding (PKG-3: cchardet never provided the `chardet` module the
# parser imports, so Linux installs were broken; single source below)
chardet>=5.0.0

# Compression support (for aiohttp brotli content-encoding)
brotli>=1.1.0
brotlicffi>=1.1.0

# Security (transitive for requests/aiohttp, pin for reproducibility)
certifi>=2024.0.0
cryptography>=43.0.0
